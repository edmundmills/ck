# Rust
target/
**/*.rs.bk

# ck index files
.ck/
.ckignore
# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Debug
*.pdb

.fastembed_cache
.cache

# MCP config files (user-specific)
.mcp.json
*mcp_config*.json

# Documentation site (Node.js)
docs-site/node_modules/
docs-site/.vitepress/dist/
docs-site/.vitepress/cache/
docs-site/.vitepress/.temp

# Claude Code local settings
.claude/
docs/tasks

# npm distribution (root) — produced by scripts/install.js
dist/
node_modules/
package-lock.json

# Embedding model blobs are fetched from HuggingFace by CI at release time
# (futur/Qwen3-Embedding-0.6B-model2vec-onnx). The reranker is checked in
# directly (small, no clean canonical source).
ck-embed/models/embedding.onnx
ck-embed/models/embedding-tokenizer.json
